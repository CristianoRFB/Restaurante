'use client';

import { CalendarDays, Clock3, LockKeyhole, MessageCircle, Users, UserRound, ArrowRight } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { LinkButton, Button } from '@/components/baru-ui';
import { PageHero, PublicLayout } from '@/components/public-shell';
import { createReservationAsync } from '@/lib/baru-repository';
import { photoUrls, settings } from '@/lib/baru-data';
import { normalizeWhatsapp, validateReservation } from '@/shared/baru-domain';

export default function ReservePage() {
  const [form, setForm] = useState({ date: '2026-09-24', time: '', partySize: '2', name: '', whatsapp: '', note: '' });
  const [error, setError] = useState('');
  const [submitting, setSubmitting] = useState(false);
  const set = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (submitting) return; const input = { date: form.date, time: form.time, partySize: Number(form.partySize), customerName: form.name, whatsapp: form.whatsapp, note: form.note }; const errors = validateReservation(input, settings); if (errors.length) { setError(errors[0]); return; } setError(''); setSubmitting(true); try { const reservation = await createReservationAsync({ date: form.date, time: form.time, partySize: Number(form.partySize), customerId: 'guest', customerName: form.name.trim(), whatsapp: normalizeWhatsapp(form.whatsapp), note: form.note.trim(), status: 'NEW', source: 'SITE' }); window.location.href = `/reserva/${reservation.code}`; } catch { setError('Não foi possível enviar agora. Tente novamente.'); setSubmitting(false); } };
  return <PublicLayout><main className="reservation-shell"><PageHero eyebrow="{settings.city}" title="Reservar mesa" description="Boa comida aproxima pessoas. Garanta seu lugar e viva uma experiência única no Baru." imageUrl={photoUrls.table} /><section className="public-section"><div className="reservation-card"><form className="reservation-form" onSubmit={submit} noValidate><div><p className="eyebrow">1 · Seus dados</p><h2 className="font-editorial" style={{ margin: '10px 0 0', fontSize: 32 }}>Conte um pouco sobre a sua visita.</h2></div><div className="form-row"><label className="form-label"><span>Data</span><span className="field-shell"><CalendarDays size={17} /><input type="date" value={form.date} min="2026-09-21" onChange={(event) => set('date', event.target.value)} required /></span></label><label className="form-label"><span>Horário</span><span className="field-shell"><Clock3 size={17} /><select value={form.time} onChange={(event) => set('time', event.target.value)} required><option value="">Selecione um horário</option>{['12:00', '12:30', '13:00', '13:30', '19:00', '19:30', '20:00', '20:30', '21:00'].map((time) => <option key={time}>{time}</option>)}</select></span></label></div><label className="form-label"><span>Pessoas</span><span className="field-shell"><Users size={17} /><select value={form.partySize} onChange={(event) => set('partySize', event.target.value)}>{Array.from({ length: settings.maxPartySize }, (_, index) => <option key={index + 1} value={index + 1}>{index + 1} {index === 0 ? 'pessoa' : 'pessoas'}</option>)}</select></span></label><label className="form-label"><span>Nome completo</span><span className="field-shell"><UserRound size={17} /><input value={form.name} onChange={(event) => set('name', event.target.value)} placeholder="Seu nome completo" required /></span></label><label className="form-label"><span>WhatsApp</span><span className="field-shell"><MessageCircle size={17} /><input value={form.whatsapp} onChange={(event) => set('whatsapp', event.target.value)} placeholder="(45) 9 9988-7766" inputMode="tel" required /></span></label><label className="form-label"><span>Observação <span className="muted">(opcional)</span></span><textarea value={form.note} onChange={(event) => set('note', event.target.value)} maxLength={500} placeholder="Alguma preferência, celebração ou observação?" /><span className="muted" style={{ textAlign: 'right', fontSize: 11 }}>{form.note.length}/500</span></label>{error && <p className="field-error" role="alert">{error}</p>}<Button type="submit" className="button-wide" disabled={submitting}>{submitting ? 'Enviando…' : <>Solicitar reserva <ArrowRight size={16} /></>}</Button><p className="form-note"><LockKeyhole size={14} /> Sua reserva será confirmada pela equipe.</p></form><aside className="reservation-summary"><img src={photoUrls.table} alt="Mesa preparada no restaurante Baru" /><h2>Sua reserva</h2><div className="summary-line"><CalendarDays size={17} /><span>Escolha uma data</span></div><div className="summary-line"><Clock3 size={17} /><span>Escolha um horário</span></div><div className="summary-line"><Users size={17} /><span>{form.partySize} pessoas</span></div><div className="summary-line"><UserRound size={17} /><span>{form.name || 'Seu nome'}</span></div><p className="quote" style={{ color: 'var(--ink)', borderColor: 'var(--terracotta)' }}>“{settings.tagline}”</p></aside></div><div className="whatsapp-card"><div><strong>Prefere falar com a gente?</strong><p>Tire dúvidas ou faça sua reserva pelo WhatsApp.</p></div><LinkButton href={`https://wa.me/${settings.whatsapp}`} variant="secondary"><MessageCircle size={17} /> Chamar no WhatsApp</LinkButton></div></section></main></PublicLayout>;
}
